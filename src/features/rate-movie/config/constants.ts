/** Диапазон допустимой оценки в форме заметки — ужесточение `collectionNoteSchema` (там оценка опциональна). */
export const RATING_RANGE = { min: 1, max: 10 } as const;

/** Ограничение длины текста заметки — защита от случайной вставки огромного текста, не проверка бизнес-правила. */
export const MAX_NOTE_TEXT_LENGTH = 2000;

/** Ограничение длины одного тега — чипы не должны превращаться в абзац. */
export const MAX_TAG_LENGTH = 30;
