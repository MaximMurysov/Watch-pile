/**
 * Курируемый список — показывается на странице поиска, пока запрос
 * пустой, чтобы экран не оставался пустым. Не «новинки проката»: у
 * poiskkino.dev сортировка по дате премьеры/рейтингу/голосам на
 * практике стабильно уходит в таймаут (не индексирована на бесплатном
 * тарифе), а единственная быстрая сортировка — по `year` — отдаёт
 * малоизвестные записи, а не узнаваемые фильмы (проверено вручную).
 * Поэтому вместо «новинок» — вручную подобранные и проверенные через
 * реальный API id известных фильмов.
 */
export const FEATURED_MOVIE_IDS = [
  301, // Матрица (1999)
  474, // Гладиатор (2000)
  2213, // Титаник (1997)
  448, // Форрест Гамп (1994)
  325, // Крёстный отец (1972)
  342, // Криминальное чтиво (1994)
  435, // Зелёная миля (1999)
  328, // Властелин колец: Братство кольца (2001)
  258687, // Интерстеллар (2014)
  447301, // Начало (2010)
] as const;

/**
 * Задержка запроса каждой следующей карточки подборки (мс) —
 * см. комментарий в featured-movie-card.tsx.
 */
export const FEATURED_CARD_STAGGER_MS = 80;
