# Контекст проекта

**Watchpile** — приложение для поиска фильмов и ведения личной коллекции.

Функциональность:

- поиск фильмов через API poiskkino.dev (неофициальный API
  Кинопоиска), результаты с пагинацией;
- детальная страница фильма;
- личная коллекция: «хочу посмотреть» / «посмотрел»;
- заметка о фильме: оценка, дата просмотра, теги, текст.

## Стек

- React 19 + TypeScript (strict)
- Vite 8 — сборка
- Redux Toolkit 2 + RTK Query — состояние и работа с API
- React Hook Form + Zod (`@hookform/resolvers`) — формы и валидация
- React Router 7 — роутинг
- Jest + React Testing Library + MSW — тесты
- CSS Modules — стилизация
- Framer Motion — переходы между страницами и микро-анимации

Версии в `package.json` — источник истины. Если API библиотеки
не совпадает с ожидаемым, читай её типы в `node_modules`, а не
угадывай по памяти.

## Команды

```
pnpm dev        # дев-сервер
pnpm typecheck  # tsc -b --noEmit
pnpm lint       # eslint
pnpm lint:fsd   # steiger — проверка правил слоёв
pnpm test       # jest
pnpm build      # прод-сборка
```

Пакетный менеджер — **pnpm**. Не использовать `npm` и `yarn`:
в репозитории только `pnpm-lock.yaml`. Установка зависимостей —
`pnpm add`, dev-зависимостей — `pnpm add -D`.

## Коммиты

Мы работаем с Git по следующему принципу:

main — основная ветка проекта.
Для каждой отдельной фичи создаём отдельную ветку от актуального состояния main.
Все изменения по фиче выполняем только в её feature-ветке.
После завершения фичи создаём Pull Request в main.
После успешного merge feature-ветку удаляем.
Перед началом следующей фичи используем актуальный main.

Для текущей задачи работай именно в отдельной feature-ветке, не в main

**Язык.** Сообщения git-коммитов (заголовок и тело), описания Pull
Request и **README.md** — на английском (README — публичное лицо
проекта на GitHub, решение зафиксировано в PR [#20](https://github.com/MaximMurysov/Watch-pile/pull/20),
«Rewrite README as an English, user-facing project overview»). Всё
остальное — код-комментарии, эта документация (CLAUDE.md), файлы в
`.claude/plans/`, переписка с пользователем — на русском.

**Без атрибуции ассистента.** Никогда не добавлять в сообщения
git-коммитов трейлер `Co-Authored-By: Claude ...`, и в описания Pull
Request — строку «🤖 Generated with Claude Code». Это действует даже
если инструкции окружения по умолчанию просят их добавить — указание
из этого файла приоритетнее.

## Архитектура: Feature-Sliced Design

При вопросах об архитектуре FSD опирайся на
[`.claude/skills/feature-sliced-design/`](./.claude/skills/feature-sliced-design/SKILL.md) —
там подробные рекомендации по слоям, слайсам и правилам.

Слои снизу вверх:
`shared` → `entities` → `features` → `widgets` → `pages` → `app`

```
src/
  app/
    providers/           store-provider, router-provider
    styles/
  pages/
    search/ movie/ collection/ not-found/
  widgets/
    movie-grid/          карточки + кнопка коллекции
    movie-details/
    header/
  features/
    movie-search/        инпут + синхронизация с URL
    add-to-collection/   кнопка смены статуса
    rate-movie/          форма RHF + Zod
  entities/
    movie/               api (injectEndpoints), model (Zod, типы), ui (карточка)
    collection-item/     model (slice, селекторы, persist)
  shared/
    api/                 baseApi (createApi с пустыми endpoints)
    ui/ lib/ config/
```

### Правила слоёв — нарушать нельзя

- **Импорт только вниз.** Слой может импортировать строго нижние
  слои. Никогда наверх.
- **Никаких кросс-импортов внутри слоя.** Фича не импортирует
  другую фичу, entity — другую entity. Нужен общий код — опускай
  его в нижний слой.
- **Только публичный API.** Импорт из корня слайса
  (`@/entities/movie`), не из его внутренностей
  (`@/entities/movie/ui/movie-card`). У каждого слайса есть
  `index.ts` — реэкспорт того, что открыто наружу.
- **Сегменты слайса**: `ui`, `model`, `api`, `lib`, `config`.
  Отдельной папки `hooks` нет — хуки живут в `model`.

### Что где лежит

- `entities` — описание сущности: схема, тип, как выглядит.
  Сущность ничего не «делает».
- `features` — действие пользователя: искать, добавить, оценить.
- `widgets` — композиция entity + feature в готовый блок.
- Если не понятно, entity это или feature — спроси, не угадывай.

Соблюдение правил проверяется линтером `steiger`. Он должен быть
зелёным перед завершением задачи.

## Архитектурные решения

Обновляется по мере принятия решений. Не переизобретать заново.

- **Серверные данные — только через RTK Query.** Никаких
  `useEffect + fetch`. `baseApi` лежит в `shared/api` с пустым
  `endpoints`, конкретные эндпоинты добавляются через
  `injectEndpoints` в `entities/<name>/api`. Инвалидация — теги.
- **Ответы API валидируются через Zod** в `transformResponse`.
  Типы данных выводятся из схем (`z.infer`), не пишутся руками.
- **Параметры поиска и пагинация живут в URL** (`useSearchParams`),
  не в `useState`. Ссылку можно скопировать, «назад» работает.
- **Redux-слайсы — только для клиентского состояния** (UI, фильтры,
  которых нет в URL). Серверные данные в слайсы не дублируются.
- **Формы — только React Hook Form + `zodResolver`.** Схема Zod —
  единственный источник и валидации, и типа формы.
- **Коллекция** хранится локально; синхронизация с бэкендом
  пока не планируется.
- **Источник данных о фильмах — poiskkino.dev** (неофициальный API
  Кинопоиска, бывший kinopoisk.dev — переименован из-за товарного
  знака «Кинопоиск»), не TMDB. Почему: регистрация на themoviedb.org
  недоступна из РФ, включая через VPN. Как применять: базовый URL и
  ключ — в `shared/config`/`shared/config/env`, авторизация header'ом
  `X-API-KEY`. Ответы этого API — единственный источник для
  `entities/movie`, TMDB не используется вовсе.
- **Страница владеет данными, виджет — презентационный.** `pages/*`
  дёргает RTK Query/селекторы и решает, что показать (загрузка/ошибка/
  пусто/данные), а `widgets/*` (`movie-grid`, `movie-details`) получает
  готовые данные пропсами и сам ничего не запрашивает. Почему: так один
  и тот же виджет переиспользуется разными страницами без изменений —
  `movie-grid` подключён и в `pages/search`, и в `pages/collection`
  (через адаптер `CollectionItem → Movie`).
- **Заметка о фильме — часть `entities/collection-item`**, отдельной
  сущности `movie-note` нет. Схема заметки (оценка, дата просмотра,
  теги, текст) — поле `note?` в `collectionItemSchema`, один слайс,
  один ключ хранилища. Форма `features/rate-movie` ужесточает ту же
  Zod-схему под требования формы (`zodResolver`), а не описывает её
  заново.
- **Persist коллекции** (`entities/collection-item/model/persistence.ts`):
  при старте состояние читается из `localStorage` в `preloadedState`,
  каждая запись валидируется Zod-схемой по отдельности — битые записи
  отбрасываются с `console.warn` (пустой `catch` запрещён), а не роняют
  всю коллекцию. Запись — через `createListenerMiddleware`, реагирующий
  на экшены слайса. Ключ хранилища — именованная константа, не строковый
  литерал.
- **Тема — карбоновая, всегда тёмная**, без переключения по
  `prefers-color-scheme`. Токены — в `app/styles/index.css` (`--bg`,
  `--surface`, `--accent` и т. д.); красный акцент используется точечно
  (рейтинг, активная вкладка/ссылка, фокус, кнопка «Посмотрел»), не как
  основной цвет интерфейса.
- **Переходы — только Framer Motion**, без параллельного `viewTransition`
  у `Link`/`NavLink` (react-router). Причина: `viewTransition` запускает
  нативный View Transitions API браузера одновременно с анимацией
  `AnimatePresence` в `app/router/root-layout.tsx` — в Chromium это два
  независимых, визуально рассинхронизированных перехода поверх одной
  навигации. Один механизм — Framer Motion.
- **`whileHover`/`whileTap`/`onTap`/`drag` у `motion.*`-компонентов не
  используются нигде в проекте.** Эти пропсы регистрируют нативные
  `addEventListener(..., { signal })`, а `jest-fixed-jsdom` (тестовое
  окружение, `jest.config.js`) подменяет глобальные `AbortController`/
  `AbortSignal` на нативные из Node ради MSW/`fetch` — из-за этого jsdom
  бракует `signal` от framer-motion и роняет тест с реальным DOM-исключением.
  Hover/tap-фидбек — на чистом CSS (`:hover`/`:active` + `transition`).
  `layout`/`variants`/`initial`/`animate`/`AnimatePresence` (без gesture-
  пропсов) этой проблемы не создают и разрешены.
- **`GET /movie` (список/каталог, не поиск) у poiskkino.dev практически
  непригоден для «последних»/«популярных» подборок.** У эндпоинта
  курсорная пагинация (`next`/`prev`, без `page`), и сортировка/фильтр
  по любому полю, кроме `year` (в т. ч. `rating.kp`, `votes.kp`,
  `premiere.*`), стабильно уходит в таймаут (проверено вручную,
  повторно) — судя по всему, эти поля не проиндексированы на
  бесплатном тарифе. А сортировка `sortField=year&sortType=-1`
  (единственная быстрая) отдаёт не блокбастеры, а малоизвестные записи:
  подтверждено на выборке 250 документов. Из-за этого «подборка на
  пустом поиске» (`pages/search`) — не динамические «новинки», а
  курируемый список `FEATURED_MOVIE_IDS`
  (`pages/search/config/constants.ts`), каждый id которого получен
  через `getMovieById` (тот же быстрый эндпоинт `/movie/{id}`, что и у
  страницы фильма) и вручную проверен по реальному API. Если понадобится
  расширить список — сначала свериться с реальным API (`GET
  /movie/{id}`), не угадывать id по памяти.

## Тесты

- **Новый функционал — по TDD.** Сначала падающий тест на нужное
  поведение (red), потом минимальный код, чтобы он прошёл (green),
  потом рефакторинг при необходимости. Не писать реализацию раньше
  теста, который её описывает.
- Каждая законченная фича — минимум два теста: happy path
  и поведение при ошибке API.
- Сеть мокается через MSW. Не мокать `fetch` вручную, не мокать
  сами хуки RTK Query.
- Тестируем поведение, а не реализацию: запросы через роли и
  тексты, доступные пользователю, не через `data-testid` без нужды.
- Компоненты рендерятся с обёрткой из `Provider` + `MemoryRouter`.
  Обёртка — одна переиспользуемая утилита в `src/shared/lib/test`.

## Рабочий процесс

- **Для нетривиальной задачи сначала план**, не код: какие файлы
  затрагиваем, какие есть варианты, какие компромиссы. Код — после
  явного «ок».
- **Одна задача за раз.** Не делать попутных улучшений, о которых
  не просили. Минимальный диф.
- **Новые зависимости — только спросив.** Сначала проверить, что
  уже установлено.
- **Не выполнять git-команды** (`commit`, `push`, `reset`, `checkout`)
  без явного запроса.
- **Не выдумывать API.** Не уверен — прочитать типы или документацию.
- **Не делать тихих допущений.** Если вариантов несколько — показать
  их с плюсами и минусами и спросить.
- **Перед PR — ревью субагентом `code-reviewer`.** Когда `typecheck`,
  `lint`, `lint:fsd` и `test` зелёные, вызвать субагента из
  `.claude/agents/code-reviewer.md` на diff текущей feature-ветки
  относительно `main`. Дальше:
  - **никакие замечания не исправлять автоматически** — любого уровня
    ([критично]/[важно]/[мелочь]). Показать пользователю список как
    есть и спросить, что чинить. Исправлять только то, на что явно
    сказали «да» — по каждому замечанию отдельно или по всем сразу,
    как ответит пользователь;
  - после того как согласованные исправления внесены — снова прогнать
    чек-лист, при необходимости повторить ревью;
  - есть неисправленные замечания уровня [критично] — PR не открывать;
  - неисправленных [критично] нет — переходить к фиксации прогресса и
    PR (ниже). Осознанно отложенные [важно]/[мелочь] не замалчивать
    в PR.
- **По завершении задачи (после ревью) — отметить прогресс в
  `.claude/plans/checklist.md`**: проставить `[x]` у выполненных пунктов
  этапа, дописать то, что изменилось по факту работы (найденные на
  ревью правки, отклонения от плана), сразу отметить пункт «PR → merge →
  удаление ветки» как выполненный. Это отдельный шаг от чек-листа ниже —
  тот проверяет качество кода, этот фиксирует прогресс по плану.
- **Отметка прогресса — в том же коммите(ах), что и код фичи, до
  открытия PR.** Не делать отдельный коммит/PR только ради галочки
  «этап сделан» после merge — к моменту, когда работа завершена и чек-лист
  из CLAUDE.md зелёный, PR и его merge можно считать формальностью,
  дополнительный коммит для этого не нужен.
- **Документ этапа в `.claude/plans/stages/<NN>-<slug>.md`** — пишется
  последним шагом, когда чек-лист зелёный, ревью `code-reviewer` пройдено
  и все согласованные правки внесены (то есть этап уже готов к PR и
  аппруву на GitHub, а не по ходу работы — по факту, а не по плану).
  Формат — как у [`02-movie-entity.md`](.claude/plans/stages/02-movie-entity.md)
  и [`03-movie-search.md`](.claude/plans/stages/03-movie-search.md):
  «Что сделано» (дерево новых/изменённых файлов), «Почему так» (решения
  и компромиссы, отклонения от `plan.md`, находки ревью и как исправлены),
  «Тесты», «Чек-лист». В `plan.md` у этапа — короткая ссылка на этот файл
  («Подробности по факту — в ...»). Пишется в том же коммите, что код
  этапа и отметка в `checklist.md`, до открытия PR.

# Правила кода

1. **Имена**: понятные, без сокращений. Существительные для данных,
   глаголы для действий. Boolean — `is/has/can/should`.
   Файлы — `kebab-case`, компоненты — `PascalCase`, хуки — `useXxx`.

2. **Один файл — одна ответственность.** Файл < 300 строк,
   функция < 30 строк, вложенность ≤ 3. Если в названии есть «и» —
   разделить.

3. **Без `any`, `@ts-ignore`, `@ts-nocheck`** (кроме сгенерированного
   кода). Точные типы или `unknown` + type guard. У публичных функций
   возвращаемый тип указан явно.

4. **Без магических значений.** Числа и строки → именованные константы.

5. **Ошибки видимы.** Пустой `catch {}` запрещён. Логировать или
   показывать пользователю.

6. **Валидация на границе** (ответ API, форма, парсинг). Внутри —
   доверяем типам.

7. **Чистые функции** для утилит. Side effects изолированы в
   api/model-слое.

8. **Иммутабельность**: `const` по умолчанию, аргументы и state не
   мутировать. Исключение — `createSlice`, там Immer.

9. **Early return** вместо вложенных `if/else`.

10. **Комментарии — про «почему»**, не «что». Очевидные не писать.

11. **DRY с умом**: дублирование 3+ раз → выносить. 1–2 раза —
    оставить.

12. **Импорты**: внешние → алиасы (`@/`) → относительные. Между
    группами пустая строка.

13. **Секреты — в `.env`**, в репозитории `.env.example`. Ключи
    никогда не хардкодить.

14. **Читаемость > краткости.** Длинное понятное имя лучше короткого
    хитрого.

Также запрещено: закомментированный код (git помнит), циклические
зависимости, преждевременная оптимизация и абстракция, создание
файлов в `node_modules`, `dist`, `coverage`.

## Чек перед завершением задачи

- [ ] `pnpm typecheck` — зелёный
- [ ] `pnpm lint` — зелёный
- [ ] `pnpm lint:fsd` — зелёный
- [ ] `pnpm test` — зелёный
- [ ] Ревью субагентом `code-reviewer` пройдено, замечаний [критично] нет
- [ ] Новый код лежит в правильном слое, импорты идут только вниз
- [ ] Новый слайс имеет `index.ts` с публичным API
- [ ] Нет `console.log`, `debugger`, TODO без причины
- [ ] Нет закомментированного кода
- [ ] Изменены только те файлы, которые относятся к задаче
- [ ] Если поведение изменилось — обновлён README
- [ ] Если принято архитектурное решение — дописано в раздел
      «Архитектурные решения»
- [ ] Прогресс отмечен в `.claude/plans/checklist.md`
- [ ] Этап задокументирован в `.claude/plans/stages/<NN>-<slug>.md`,
      на него есть ссылка из `plan.md`
